# Properties file for algorithm-testing environment
problemValueType=min
limitExecTime=false
maxExecTimeSeconds=3
displayTime=true
display=false
printSummary=true


# Currently supported problem types: setDiameter, sorting
problemType=MTSP
MTSPTester.testLarge=true

# Sorting stuff:
dictionary=/usr/dict/words
numProblemParameters=2

# How many algorithms, and where each algorithm is located.
numAlgorithms=3

# Full path name to algorithm:
alg0=Naive
alg1=Greedy
alg2=Annealing

# Output scaling: one function per algorithm
# Scale functions:
#   F0(n) = 1.0 (no scaling)
#   F1(n) = A*n
#   F2(n) = A*n^2
#   F3(n) = A*n^3 
#   F4(n) = A*log(n)
#   F5(n) = A*n*log(n)
#   F6(n) = A*sqrt(n)
A0=1.0
scale0=F0
A1=1.0
scale1=F0
A2=1.0
scale2=F0
A3=1.0
scale3=F0
A4=1.0
scale4=F0
A5=1.0
scale5=F0

# test for correctness?
testCorrectness=true

# special tests?
specialTest=true

# test for performance?
testPerformance=false
# Test sizes for performance testing.
numTests=5
param0_testSize0=50000
param0_testSize1=125000
param0_testSize2=250000
param0_testSize3=400000
param0_testSize4=500000

param1_testSize0=50000
param1_testSize1=125000
param1_testSize2=250000
param1_testSize3=400000
param1_testSize4=500000

# Use an average of how many data sets per test size?
runsPerTest=5

# Print stuff to screen.
printToScreen=false

display=false
debug=On
# Time to wait for garbage collection.
quiescentTime=1
